# M3 — Program Basics

**Phase:** D · **Weeks:** ~Weeks 3–4

**Big idea:** State — a program holds a changing world. Variables, types, I/O, arithmetic.

> Verbatim from the spine (`_storming/CSC-134-course-spine.md`, "## M3 — Program Basics"):
> variables and data types; `cin`/`cout`; arithmetic and expressions; the
> **single-file convention** introduced (prototypes-top / definitions-bottom is
> filled in once functions arrive in M6 — M3 code lives entirely in `main`);
> meaningful names and comments as communication. **Debugging as curriculum:**
> the first error is a *planned, celebrated event* — we break a working program
> and read the compiler/runtime message together; failure is exercise, put in
> the reps.

---

## LPAA beat map

| Beat | One-liner (from spine) |
|---|---|
| **Learn** | `thinkcpp` / *Chapter 2* reading on I/O and expressions. |
| **Practice** | Exit ticket — trace an expression; predict I/O. |
| **Apply** | Instructor-led — type in an input → process → output program; get it building and running clean. |
| **Assess** | A small I/O + arithmetic program from spec (the Pizza Calculator project slots here). |

Full acceptance-criteria skeleton for the Assess beat lives in `_assess-spec.STUB.md`
(this file only carries the one-liner). MLOs live in `_mlos.md`. Asset provenance
and the legacy-content ledger live in `_assets.md`.

---

## Make-gradient position

**M3 sits in the M2–M4 band: type-in 100%.** The Apply beat hands students the
full program to type in, character by character, and get it compiling and
running clean — no scaffolding gaps to fill (that starts at M5). Do not drift
this position when you write the Apply tutorial. A "here's 80%, finish it"
shape belongs to M5–M7, not M3.

---

## User story (fill-in — deep-build authors this against the actual M2 exit state)

> As a student finishing M2 (How to Solve Problems), I want ________________,
> so that ________________.

*(Seed thought, not the authored answer: M2 leaves a student able to read a
Hello-World-shaped program, name an error by the four-word taxonomy, and reason
in flowcharts/pseudocode. But that student has no way yet to make a program
hold or react to a changing value. M3 is where the program starts remembering
something.)*

---

## Canonical home

**This file, and the rest of `modules/m3/`, is the canonical spine-truth home
for M3.** `assignments/` is legacy source — frozen, not a scaffold target. See
`_assets.md` for exactly which legacy files hold M3's pre-existing content, and
what porting them into this tree will require. Nothing under `assignments/`
was moved, renamed, or edited to produce this scaffold.

---

## Contracts touched

None. This is a structure-only skeleton pass — no Learn/Practice/Apply/Assess
content authored, no edits to `_contracts/`, `_storming/`, `_tracking/`, or
`assignments/`.
