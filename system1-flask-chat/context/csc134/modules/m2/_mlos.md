# M2 — Module Learning Objectives (skeleton)

> **SKELETON.** These are MLO *slots* feeding the course CLOs, not authored,
> polished objectives. Deep-build fills in verbs/conditions/criteria against
> `reading-generator` / `exit-ticket-generator` / `apply-tutorial-generator` /
> `lab-creator` output. Do not treat this list as final phrasing.

## MLO slots

Draft from the spine's M2 **Content** and **LPAA** lines
(`_storming/CSC-134-course-spine.md`, ~line 136–158). Each slot names the
*topic*, not the finished objective sentence.

- **MLO-M2.1** — [TBD] Explain why multiple programming languages exist
  (the Hello World tour: HTML/CSS → JS → Python → C++ → a slice of assembly —
  same output, different writing).
- **MLO-M2.2** — [TBD] Distinguish compiler vs. interpreter, and identify the
  C++ build step as a physical, inspectable artifact (the SDLC arc's Compile
  node).
- **MLO-M2.3** — [TBD] Read a flowchart (Mermaid) and a piece of pseudocode;
  produce a simple one from a plain-language process description.
- **MLO-M2.4** — [TBD] Write a user story in the "As a ___, I want ___, so
  that ___" form.
- **MLO-M2.5** — [TBD] Classify a given error into the four-word taxonomy
  (Syntax / Static semantic / Runtime / Logic) and state which taxonomy
  member(s) can only be caught which way (compile vs. run vs. never, since
  logic errors "work").
- **MLO-M2.6** — [TBD] Predict the output of a short, provided program before
  running it (`thinkcpp` Predict-the-output checkpoint).
- **MLO-M2.7** — [TBD] Given a working program, describe its behavior in
  plain language and recover its flowchart (Perspective Flip — feeds the
  Assess beat directly).

---

## CCL crosswalk touch

Per the spine's CCL crosswalk table (`## CCL crosswalk (compliance anchor)`,
~line 338), **M2 has no dedicated CCL topic row.** M3 (I/O, arithmetic), M4
(filters), M5 (iteration), and M7 (arrays, pointers, OOP) each get one — M2
doesn't.
M2's spine section itself carries no explicit `**CCL touch:**` line either —
its own **Spine connection** note instead reads: *"the module the course
orbits — M8's rubric grades exactly these artifacts."*

Skeleton read on where M2 touches the CCL crosswalk anyway:

- Feeds the crosswalk's catch-all row **"Design, code, test, debug"** — the
  whole-spine CCL closing clause. It does this by building the vocabulary
  (flowcharts, pseudocode, user stories, the four-word error taxonomy) that
  every later module's CCL-mapped content (M3 I/O, M4 filters, M5 iteration,
  M7 arrays/pointers/OOP) is *communicated* and *debugged* in.
- No CCL topic bullet to fill in here — **skeleton note, not a gap to
  silently patch.** If a future ruling wants M2 to carry an explicit CCL row,
  that is a spine change, not a module-builder call.
