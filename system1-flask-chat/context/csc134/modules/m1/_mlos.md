# M1 — Module Learning Objectives (skeleton)

> **Skeleton slots only — not authored objectives.** This file names the
> *shape* each MLO must take and the CLO/CCL it feeds. The deep-build pass
> writes the actual objective language.

## MLO slots (feeding the CLOs)

Fill each slot with a single, observable, verb-first objective. One slot per
LPAA beat's core competency; do not merge slots.

| Slot | Feeds CLO # (TBD) | Draft shape (fill in deep-build) |
|---|---|---|
| MLO-1 | — | *By the end of M1, students can explain why plain-text formats (Markdown) are the shared language of both code and professional collaboration.* |
| MLO-2 | — | *By the end of M1, students can read and write basic Markdown syntax (headers, lists, links, code fences) and predict how it renders.* |
| MLO-3 | — | *By the end of M1, students can execute the repo → edit → commit → push → verify-on-GitHub cycle without branching.* |
| MLO-4 | — | *By the end of M1, students can decompose a written task into an ordered, precise set of steps (the Robot Sandwich competency), sufficient for someone else to execute without guessing.* |

*(CLO numbers are TBD pending the course-level objectives document — this
skeleton does not invent CLO numbering.)*

---

## CCL-crosswalk touch

Per the spine's CCL crosswalk table (`_storming/CSC-134-course-spine.md`,
line ~338), M1 has no named CCL topic row of its own. Its touch is indirect
and foundational: M1 sits upstream of the CCL's coding topics, in the
**Communication** strand:

| CCL element | M1's touch |
|---|---|
| (none of the CCL's I/O / iteration / arithmetic / arrays / pointers / filters / OOP rows land in M1) | M1 is pre-code: it builds the Communication/Decomposition competency (spine's competency-thread table, line ~88, ~92) that every later CCL-coded module depends on. |
| Design, code, test, debug (CCL summative line) | M1 seeds "design" — decomposition into precise, ordered steps — before any code exists. |

**Skeleton flag:** if a future CCL revision adds an explicit "written
communication / documentation" row, M1 is its natural anchor. Record that
possibility here — don't add the row yet. This file doesn't amend the CCL
table; that lives in the spine.

---

## Cross-references

- Spine competency-thread table: `_storming/CSC-134-course-spine.md` lines
  ~86–92 (Decompose into steps → M1; Communicate the result → M1).
- CCL crosswalk table: `_storming/CSC-134-course-spine.md` line ~338.
- Module identity: `modules/m1/_overview.md`.
